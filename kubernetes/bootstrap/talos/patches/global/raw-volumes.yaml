apiVersion: v1alpha1
kind: RawVolumeConfig
name: lvm-data
provisioning:
  diskSelector:
    match: disk.model.startsWith("Micron_7450")
  minSize: 250GiB
  maxSize: 250GiB
---
apiVersion: v1alpha1
kind: RawVolumeConfig
# do not put "ceph" in the name otherwise ceph isn't going to create an osd
name: rook-data
provisioning:
  diskSelector:
    match: disk.model.startsWith("Micron_7450")
  minSize: 250GiB
