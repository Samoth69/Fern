---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: harbor
spec:
  interval: 30m
  chart:
    spec:
      chart: harbor
      version: 1.18.2
      sourceRef:
        kind: HelmRepository
        name: harbor
      interval: 5m
  values:
    externalURL: https://harbor.fern.samoth.eu
    expose:
      ingress:
        hosts:
          core: harbor.fern.samoth.eu
        className: traefik-internal
      # we do not want harbor internal cert
      tls:
        enabled: false
    ipFamily:
      ipv6:
        enabled: false
    database:
      type: external
      external:
        host: postgres-infra-local-primary
        username: harbor
        coreDatabase: harbor
        existingSecret: postgres-infra-local-pguser-harbor
        sslmode: require
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
    trivy:
      resources:
        requests:
          cpu: 10m
          memory: 256Mi
    redis:
      type: external
      external:
        addr: harbor-dragonfly:6379
        existingSecret: harbor-redis-secret
    core:
      replicas: 1
    registry:
      replicas: 1
    persistence:
      persistentVolumeClaim:
        registry:
          existingClaim: harbor-registry
        jobservice:
          jobLog:
            storageClass: openebs-hostpath
        trivy:
          storageClass: openebs-hostpath
